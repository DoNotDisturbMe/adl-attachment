{
  "name": "azure-logic-app-email-to-adls",
  "version": "1.0.0",
  "description": "A Logic App that triggers on email, downloads attachments, and uploads them to Azure Data Lake Storage.",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "az logic app deployment create --resource-group <your-resource-group> --name <your-logic-app-name> --template-file workflows/email-trigger-workflow.json"
  },
  "dependencies": {
    "@azure/storage-file-datalake": "^12.0.0",
    "@azure/ms-rest-nodeauth": "^3.0.0",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "@types/node": "^14.0.0"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}